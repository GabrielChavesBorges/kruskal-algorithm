#include <stdio.h>
#include <stdlib.h>

struct edge {
    int start_node;
    int end_node;
    int weight;
};

int main() {
    struct edge* edges;
    int edge_count = 0;
    
    // create structs representing edges
    // apply kruskal's algorithm
    return 0;
}